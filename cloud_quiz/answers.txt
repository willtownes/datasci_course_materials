Data Science Cloud Computing Quiz
Answers
1.1 How many MapReduce jobs are generated by example.pig? 3

1.2 How many reduce tasks are within the first MapReduce job? 50. How many reduce tasks are within later MapReduce jobs? Job2: 1, Job3: 50.

1.3 How long does each job take? Job1: 4mins, Job2: 1min, Job3: 2mins. How long does the entire script take? 7mins

1.4 What is the schema of the tuples after each of the following commands in example.pig?

1.4a After the command ntriples =... {subject:chararray,predicate:chararray,object:chararray}

1.4b After the command objects =... {group:chararray,ntriples:{(subject:chararray,predicate:chararray,object:chararray)}}

1.4c After the command count_by_object =... {group:chararray,count:long}
    
What you need to turn in:
How many records are there in count_by_object? 1622294
